CREATE TABLE tweets (
id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
tweet VARCHAR(280) NOT NULL,
account VARCHAR(50) NOT NULL,
tweet_timestamp TIMESTAMP WITH TIME ZONE,
tweet_url VARCHAR(100),
UNIQUE (tweet, account)
);